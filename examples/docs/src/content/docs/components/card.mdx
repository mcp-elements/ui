---
title: Card
description: A container with header, content, and footer sections.
---

import { Tabs, TabItem } from '@astrojs/starlight/components'
import { CardDemo } from '../../../demos/CardDemo'
import ComponentPreview from '../../../components/ComponentPreview.astro'
import PropsTable from '../../../components/PropsTable.astro'

A composable card component with header, title, description, content, and footer sub-components. Ideal for grouping related content and actions.

## Preview

<Tabs>
  <TabItem label="Preview">
    <ComponentPreview>
      <CardDemo client:load />
    </ComponentPreview>
  </TabItem>
  <TabItem label="React">
    ```tsx
    import {
      Card, CardHeader, CardTitle, CardDescription,
      CardContent, CardFooter, Input, Button,
    } from '@mcp-elements/react'

    export function CardDemo() {
      return (
        <Card className="w-full max-w-sm">
          <CardHeader>
            <CardTitle>Create project</CardTitle>
            <CardDescription>Deploy your new project in one click.</CardDescription>
          </CardHeader>
          <CardContent>
            <Input placeholder="Project name" />
          </CardContent>
          <CardFooter className="flex justify-between">
            <Button variant="outline">Cancel</Button>
            <Button>Deploy</Button>
          </CardFooter>
        </Card>
      )
    }
    ```
  </TabItem>
  <TabItem label="Angular">
    ```ts
    import { Component } from '@angular/core';
    import {
      SnxCardComponent, SnxCardHeaderComponent,
      SnxCardTitleComponent, SnxCardDescriptionComponent,
      SnxCardContentComponent, SnxCardFooterComponent,
    } from '@mcp-elements/angular';
    import { SnxInputComponent } from '@mcp-elements/angular';
    import { SnxButtonComponent } from '@mcp-elements/angular';

    @Component({
      selector: 'app-card-demo',
      standalone: true,
      imports: [
        SnxCardComponent, SnxCardHeaderComponent,
        SnxCardTitleComponent, SnxCardDescriptionComponent,
        SnxCardContentComponent, SnxCardFooterComponent,
        SnxInputComponent, SnxButtonComponent,
      ],
      template: `
        <mcpe-card class="w-full max-w-sm">
          <mcpe-card-header>
            <mcpe-card-title>Create project</mcpe-card-title>
            <mcpe-card-description>
              Deploy your new project in one click.
            </mcpe-card-description>
          </mcpe-card-header>
          <mcpe-card-content>
            <mcpe-input placeholder="Project name" />
          </mcpe-card-content>
          <mcpe-card-footer class="flex justify-between">
            <mcpe-button variant="outline">Cancel</mcpe-button>
            <mcpe-button>Deploy</mcpe-button>
          </mcpe-card-footer>
        </mcpe-card>
      `,
    })
    export class CardDemoComponent {}
    ```
  </TabItem>
</Tabs>

## Installation

<Tabs>
  <TabItem label="CLI">
    ```bash
    npx mcp-elements add card
    ```
  </TabItem>
  <TabItem label="Manual (React)">
    Copy these files into your project:
    - `css/components/card.css`
    - `react/card.tsx`
  </TabItem>
  <TabItem label="Manual (Angular)">
    Copy these files into your project:
    - `css/components/card.css`
    - `angular/card.ts`
  </TabItem>
</Tabs>

## Usage

```tsx
import {
  Card, CardHeader, CardTitle,
  CardDescription, CardContent, CardFooter,
} from '@mcp-elements/react'
```

### Basic Card

```tsx
<Card>
  <CardHeader>
    <CardTitle>Notifications</CardTitle>
    <CardDescription>You have 3 unread messages.</CardDescription>
  </CardHeader>
  <CardContent>
    <p>Check your inbox for the latest updates.</p>
  </CardContent>
</Card>
```

### Card with Form

```tsx
<Card className="w-full max-w-sm">
  <CardHeader>
    <CardTitle>Create project</CardTitle>
    <CardDescription>Deploy your new project in one click.</CardDescription>
  </CardHeader>
  <CardContent>
    <Input placeholder="Project name" />
  </CardContent>
  <CardFooter className="flex justify-between">
    <Button variant="outline">Cancel</Button>
    <Button>Deploy</Button>
  </CardFooter>
</Card>
```

### Card with Content Only

```tsx
<Card>
  <CardContent>
    <p>A simple content-only card without header or footer.</p>
  </CardContent>
</Card>
```

## API Reference

### Card

<PropsTable props={[
  { name: 'className', type: 'string', description: 'Additional CSS classes on the card container.' },
  { name: 'children', type: 'ReactNode', description: 'Card sub-components (CardHeader, CardContent, etc.).' },
]} />

### CardHeader

<PropsTable props={[
  { name: 'className', type: 'string', description: 'Additional CSS classes.' },
  { name: 'children', type: 'ReactNode', description: 'Header content (CardTitle, CardDescription).' },
]} />

### CardTitle

<PropsTable props={[
  { name: 'className', type: 'string', description: 'Additional CSS classes.' },
  { name: 'children', type: 'ReactNode', description: 'Title text.' },
]} />

### CardDescription

<PropsTable props={[
  { name: 'className', type: 'string', description: 'Additional CSS classes.' },
  { name: 'children', type: 'ReactNode', description: 'Description text.' },
]} />

### CardContent

<PropsTable props={[
  { name: 'className', type: 'string', description: 'Additional CSS classes.' },
  { name: 'children', type: 'ReactNode', description: 'Main card content.' },
]} />

### CardFooter

<PropsTable props={[
  { name: 'className', type: 'string', description: 'Additional CSS classes.' },
  { name: 'children', type: 'ReactNode', description: 'Footer content (actions, buttons).' },
]} />
