---
title: Installation
description: Framework-specific installation guides for mcp-elements.
---

## React

### 1. Set up Tailwind CSS v4

```bash
npm install tailwindcss @tailwindcss/vite
```

In your Vite config:

```js
import tailwindcss from '@tailwindcss/vite'

export default {
  plugins: [tailwindcss()],
}
```

### 2. Initialize mcp-elements

```bash
npx mcp-elements init
```

### 3. Add components

```bash
npx mcp-elements add button card dialog
```

### 4. Import CSS

In your main CSS file:

```css
@import 'tailwindcss';
@import './mcp-elements/css/base.css';
@import './mcp-elements/css/components/button.css';
@import './mcp-elements/css/components/card.css';
@import './mcp-elements/css/components/dialog.css';
```

### 5. Use components

```tsx
import { Button } from './mcp-elements/react/button'
import { Dialog, DialogHeader, DialogTitle } from './mcp-elements/react/dialog'
```

## Angular

### 1. Set up Tailwind CSS v4

```bash
npm install tailwindcss
```

Add to your `angular.json` styles array:

```json
"styles": ["src/styles.css"]
```

### 2. Initialize and add components

```bash
npx mcp-elements init --framework angular
npx mcp-elements add button card
```

### 3. Use standalone components

```typescript
import { ButtonComponent } from './mcp-elements/angular/button.component'

@Component({
  standalone: true,
  imports: [ButtonComponent],
  template: `<mcpe-button variant="primary">Click</mcpe-button>`,
})
export class AppComponent {}
```

## Manual Installation

If you prefer not to use the CLI, you can copy files directly from the [mcp-elements repository](https://github.com/thepsygeek/mcp-elements):

1. Copy `packages/css/base.css` to your project
2. Copy the component CSS from `packages/css/components/`
3. Copy the React/Angular component from `packages/react/src/` or `packages/angular/src/`
4. For interactive components, also copy the core logic from `packages/core/src/`
