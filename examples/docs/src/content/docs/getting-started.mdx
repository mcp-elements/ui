---
title: Getting Started
description: Get up and running with mcp-elements in minutes.
---

## Prerequisites

- **Node.js** 20+
- **pnpm**, **npm**, or **yarn**
- **Tailwind CSS v4** configured in your project

## Quick Start

### 1. Initialize mcp-elements in your project

```bash
npx mcp-elements init
```

This creates a `mcp-elements.json` config file and sets up the required directory structure.

### 2. Add components

```bash
npx mcp-elements add button
npx mcp-elements add card input dialog
```

Components are copied directly into your project — you own the source code.

### 3. Import the CSS

In your main CSS file, import the base tokens and component styles:

```css
@import 'tailwindcss';
@import './mcp-elements/css/base.css';
@import './mcp-elements/css/components/button.css';
@import './mcp-elements/css/components/card.css';
```

### 4. Use in your code

```tsx
import { Button } from './mcp-elements/react/button'

function App() {
  return <Button variant="primary">Click me</Button>
}
```

## What gets copied?

When you run `npx mcp-elements add button`, the CLI copies:

| File | Purpose |
|------|---------|
| `css/components/button.css` | Tailwind v4 styles |
| `react/button.tsx` | React component |
| `core/button.ts` | Core logic (interactive components only) |

## Next steps

- [Installation guide](/installation/) — Framework-specific setup instructions
- [CLI reference](/cli/) — All CLI commands and options
- [Theming](/theming/) — Customize colors, dark mode, and glass theme
